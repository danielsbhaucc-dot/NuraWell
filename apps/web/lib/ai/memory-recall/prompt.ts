/** הנחיות רזות לכלי recall — שאר הפרטים בתיאור הכלי עצמו. */
export const MEMORY_RECALL_TOOL_PROMPT = `כלי recall_past_memory: רק במאבק/ניצחון/הרגל חוזר — לא בברכות. השתמש ב-when מהכלי, אל תמציא תאריכים. טבעי, לא רובוטי.`;
